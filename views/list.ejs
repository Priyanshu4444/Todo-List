<%- include("header.ejs"); %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
</head>
<body>
    <div class="box" id="heading">
        <h1> <%= todaysDay %></h1>
    </div>
    <div class="box">
        <% for(i=0; i < newItems.length; i++){%>
        <div class="item">
            <input type="checkbox">
            <p> <%= newItems[i] %> </p>
        </div>
        <% } %>
    </div>
        
        <form action="/" method="post">
            <input type="text" name="newItems" placeholder="Add Task" autocomplete="off">
            <button type="submit">+</button>
        </form>
    </ul>
</body>
</html>
<%- include("footer.ejs"); %>